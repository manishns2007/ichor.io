<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Causely uses Jetpack Compose for all UI.
        This theme is only required for the Activity window chrome
        (status bar, navigation bar) before Compose takes over.
    -->
    <style name="Theme.Causely" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
